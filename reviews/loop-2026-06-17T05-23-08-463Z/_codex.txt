{"verdicts":[{"id":0,"verdict":"valid","reason":"`dashboard/server.py` exposes multiple mutating POST routes that create tasks, rewrite files, and trigger workflows with no authn/authz layer or origin validation."},{"id":1,"verdict":"valid","reason":"`/sites/{slug}` uses a string `startswith()` containment check on a resolved path and `/api/landing/deploy` writes `SITES_DIR / slug / index.html` directly, so `..`-style sibling-prefix escapes are possible."},{"id":2,"verdict":"valid","reason":"`alpaca_paper.sync()` does a read-plan-submit-write cycle on `EXECUTED_LOG` with no cross-thread/process lock, and overlapping `run_cycle()` executions can therefore place duplicate orders."},{"id":3,"verdict":"valid","reason":"`EXECUTED_LOG` is overwritten with plain `write_text()` while `_load()` maps JSON decode failure to `[]`, so a torn/corrupt write can erase dedupe state and replay old verdicts."},{"id":4,"verdict":"valid","reason":"`build_system_prompt()` appends raw agent memory and cross-agent insight file contents straight into the system prompt, so writable vault content becomes high-trust prompt input."},{"id":5,"verdict":"valid","reason":"`_load_processed()` degrades corruption to an empty set and the brief writers do not check for existing task IDs across pipeline folders, so stale bridge files can be re-enqueued after log loss/crash."},{"id":6,"verdict":"valid","reason":"`dashboard/watcher.py` reads immediately on modify and broadcasts `{}` on parse/read failure, so non-atomic state writes can temporarily blank dashboard state for all websocket clients."},{"id":7,"verdict":"valid","reason":"`update_outreach_status()` never enforces `CALL_OUTCOMES` and writes raw status/notes back into a markdown table, so invalid values or `|`/newline characters can corrupt `crm.md`."},{"id":8,"verdict":"valid","reason":"`graded_picks()` calls `float(o.get(\"return_pct\", 0) or 0)` without catching `ValueError`, so one malformed outcome record crashes the eval path."},{"id":9,"verdict":"valid","reason":"`dispatch_tool()` raises `ValueError` for unknown tools and `AgentBase.run()` does not catch that call, so a hallucinated tool name aborts the whole task."},{"id":10,"verdict":"invalid","reason":"The holiday fallback is no longer 2026-only: `market_clock._HOLIDAYS_2026` already includes 2027 dates, so the finding’s 'outside 2026' claim overstates the bug."},{"id":11,"verdict":"valid","reason":"`runner/tools/landing.py` builds `LANDINGS_DIR / f\"{slug}.json\"` with no slug validation or containment check, allowing `../` path escape."},{"id":12,"verdict":"valid","reason":"`runner/ledger/market_clock.py` only hardcodes 2026-2027 holiday closures, so the ET fallback can misclassify later-year holidays if Alpaca clock data is unavailable."},{"id":13,"verdict":"valid","reason":"`grade_poc()` uses `OPP_DIR / f\"{slug}.md\"` before any ledger-match check, so a crafted slug can modify arbitrary existing `.md` files outside `vault/opportunities`."},{"id":14,"verdict":"valid","reason":"`runner/tools/files.py` uses `str(target).startswith(str(PROJECT_ROOT.resolve()))` for containment, which is bypassable by sibling paths sharing the repo prefix."}],"new":[{"severity":"high","file":"dashboard/server.py","issue":"`api_trigger()` starts an unbounded background `run_cycle()` thread per request, and `run_cycle()` has no process-wide lock, so concurrent clicks or overlap with the scheduler can execute the full cycle twice and duplicate side effects.","fix":"Serialize the entire cycle with a global/interprocess lock and coalesce manual triggers into a queued wake-up instead of spawning raw threads."},{"severity":"high","file":"runner/tools/vault_memory.py","issue":"`write_memory()` trusts `role_id` as a path segment (`AGENTS_MEMORY_DIR / role_id`), so a tool caller can use traversal like `../other_agent` to write another agent’s `memory.md` and poison future system prompts.","fix":"Validate `role_id` against the configured agent IDs and enforce resolved-path containment under `vault/agents` before writing."},{"severity":"high","file":"runner/tools/opportunity.py","issue":"`log_opportunity()` writes `OPP_DIR / f\"{slug}.md\"` with no containment check, so a crafted slug can create or overwrite arbitrary `.md` files outside `vault/opportunities`.","fix":"Restrict `slug` to a safe pattern such as kebab-case and reject any resolved path that is not contained within `OPP_DIR`."}]}